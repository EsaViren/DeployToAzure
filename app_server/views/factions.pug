extends layout

block content

  .container
    .row
      .col-12
  h1= 'World Of Warcraft Guild rankings with factions Tier 8'
  ul
    each rank in ranks
      li= rank.mones + ': ' + rank.faction + ' (' + rank.kilta + ')'

